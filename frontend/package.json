{
	"name": "frontend",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "npm run dev:wasm && vite dev --host",
		"dev:wasm": "cd .. && cargo build --target wasm32-unknown-unknown --release && cp target/wasm32-unknown-unknown/release/WebFlappyBird.wasm frontend/static/",
		"build": "npm run build:wasm && vite build",
		"build:wasm": "cd .. && cargo build --target wasm32-unknown-unknown --release && mkdir -p frontend/static/assets && cp target/wasm32-unknown-unknown/release/WebFlappyBird.wasm frontend/static/assets/WebFlappyBird-v1.0.0.wasm",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^3.0.7",
		"@sveltejs/kit": "^2.49.1",
		"@sveltejs/vite-plugin-svelte": "^6.2.1",
		"svelte": "^5.45.6",
		"svelte-check": "^4.3.4",
		"typescript": "^5.9.3",
		"vite": "^7.2.6"
	}
}
